{
  "name": "poc-playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:firefox": "npx playwright test --project=firefox",
    "test:chrome": "npx playwright test --project=chromium",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "test:allure1": "npm run test:firefox && npm run allure:generate && npm run allure:open",
    "test:allure2": "npm run test:chrome && npm run allure:generate && npm run allure:open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.14.1",
    "allure-playwright": "^3.2.1"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
